import React from 'react';
import { ArrowDown, BrainCircuit, CheckCircle2, Database, ShieldCheck, UserCheck } from 'lucide-react';

const Step = ({ icon: Icon, title, body }: { icon: any; title: string; body: string }) => <div className="rounded-lg border border-border bg-background p-4 text-center"><Icon className="mx-auto mb-2 h-6 w-6 text-primary" /><p className="font-semibold text-text">{title}</p><p className="mt-1 text-xs text-textSecondary">{body}</p></div>;

export const AIAssignmentEngine = () => <div className="p-6 space-y-6 text-text"><div><h2 className="text-2xl font-bold flex items-center gap-2"><BrainCircuit className="text-primary" />AI Assignment Engine</h2><p className="mt-2 text-textSecondary">The same pure intelligence core serves production adapters and isolated simulation scenarios. It never writes production assignments.</p></div><div className="grid gap-3 md:grid-cols-6 items-center"> <Step icon={Database} title="Structured input" body="Node reads authorized MySQL data." /><ArrowDown className="mx-auto hidden md:block text-textSecondary" /><Step icon={ShieldCheck} title="Hard constraints" body="Filter garage, leave, skills, conflicts, workload." /><ArrowDown className="mx-auto hidden md:block text-textSecondary" /><Step icon={BrainCircuit} title="Rank + optimize" body="XGBoost when trained; OR-Tools set optimization." /><ArrowDown className="mx-auto hidden md:block text-textSecondary" /><Step icon={CheckCircle2} title="Explain" body="Deterministic reasons and alternatives." /><ArrowDown className="mx-auto hidden md:block text-textSecondary" /><Step icon={UserCheck} title="Manager review" body="Node revalidates before transaction." /></div><div className="grid gap-4 md:grid-cols-2"><div className="rounded-lg border border-border p-4"><h3 className="font-semibold">Cold start safety</h3><p className="mt-2 text-sm text-textSecondary">Until enough assignment-time snapshots with outcome variation exist, ranking is deterministic and explicitly marked COLD_START. No accuracy or confidence is claimed.</p></div><div className="rounded-lg border border-border p-4"><h3 className="font-semibold">Production boundary</h3><p className="mt-2 text-sm text-textSecondary">FastAPI accepts only structured data and returns recommendations. Only authenticated Node transactions create Job_Assignment records and audit events.</p></div></div></div>;
